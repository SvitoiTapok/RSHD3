listen_addresses = '*'
wal_level = replica

max_wal_senders = 10
max_replication_slots = 5
wal_keep_size = 64MB
hot_standby = on
primary_conninfo = 'host=db1 port=5432 user=replicator password=replicator_pass application_name=db2'

